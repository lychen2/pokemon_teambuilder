# 实际验收与算法对照

验收日期：2026-09-14。应用 **0.2.0**，算法 **mc-1.3.0**。本版提供本地构筑、规则迭代和可复查分析。当前开发集中新搜索的模拟成绩低于热门配置与旧启发式，不能宣称推荐整体更强，也不能把模拟成绩当作真实排位胜率。

19 项研究优化的原始要求保存在 [OPTIMIZATION_REQUIREMENTS.md](OPTIMIZATION_REQUIREMENTS.md)，逐项实现与验收缺口见 [COMPLETION_AUDIT.md](COMPLETION_AUDIT.md)。原始报告保留失败与未完成结果，没有以功能完成替代效果证明。

## 固定输入与机器

| 项目 | 实际版本 |
|---|---|
| Showdown | `aa17ca0fac8bc5605df673bd8774c2d0e91efa43` |
| Champions calc | `e7fd7e59f3eef7ea42fba3c8b83261cb4a14109d` |
| 引擎 | `a08863c8f297-e7fd7e59f3ee` |
| 环境 | `gen9championsvgc2026regmc-b93855027bc29c2f645c` |
| 完整语料 / 随包模型 | `44cdfd0e4846242f486a` / `d839b84a9e2f096d304d` |
| 基线报告训练语料 / 模型 | `f2037e2e6158f1b2f72d` / `49caf6769ff7127f6b74` |
| 内外层校准报告模型 | `d1f86252a34b7983ebc2` |
| 新窗口报告训练模型 | `b0cc92aa690fac941d72` |
| 随包历史先验强度 / 温度 | 8 / 1.5 |
| 机器 | AMD Ryzen AI 9 H 365 w/ Radeon 880M；20 逻辑线程；30.63 GiB RAM |
| 软件 | Linux x86-64；Node.js 26.8.2；pnpm 10.32.1；Electron 44.3.0 |

完整模型、内外层留出模型与外部验证模型的训练样本不同，不能混用版本或把开发成绩当随包模型的独立盲测。报告保存各自的环境、语料、模型与算法版本。

## 数据接入与真实配置

| 赛季 | 来源 | 完整合法 | 缺字段 | 当前规则非法 | 获取失败 |
|---|---:|---:|---:|---:|---:|
| M-C | 184 | 178 | 6 | 0 | 0 |
| M-B | 861 | 751 | 108 | 2 | 0 |
| M-A | 1104 | 914 | 178 | 12 | 0 |

合计 2,149 份来源、12,894 条配置观察。模型包含 890 个真实 medoid；真实配置库保留 **8,885 份完整配置，其中 921 份有当前赛季样本支持**，覆盖 221 个物种。Dex 可搜索 293 个合法初始物种。历史配置的“非法”指按当前 M-C 重新校验失败，不表示其原规则下非法。

中文与英文均可按物种、招式、道具和特性筛选非代表配置，配点及原始来源随配置保留。去重不会把招式或配点不同的变体合并；历史资料只贡献先验，M-C 共享构筑频率独立统计。

部分观察的 1,312 份候选中，1,131 份兼容已有流派、174 份不匹配、7 份已知字段非法。未知配点与性格保持未知，合法性见证不保存为真实配置、不参与伤害精算。

## 核心、桌面与版本迁移

- 类型检查通过。
- 核心检查：**5 个文件、44 项通过，13.71 秒**，使用 60 秒总限时。该日志在修改发布版本号前生成，包名显示 0.1.0；测试内容已包含 mc-1.3.0 的机制、存储与配置变体改动。
- 普通桌面流程：**14 项通过、1 项系统输入法检查按设计跳过**。系统输入法另行实际执行，不计入普通流程通过数。
- 无 `algorithmVersion` 字段的真实旧版模型可升级；自定义语料后台重建，进度与取消入口可用。取消保留旧语料和模型，升级保留队伍及偏好。

核心检查覆盖 Champions 配点及文本往返、伤害乱数、最高乱数与必杀区别、Mega 前后特性、范围招式、天气/场地、帮助/墙、威吓免疫、烧伤/能力等级、种子轻装、顺风/空间/同速、合法性、未知字段、重复来源、字段锁、结果过期、取消及固定种子重放。

机制回归还使用真实 Showdown 状态或事件核对复生祈祷、灭歌倒数和换出重置、魔法镜、强制充能、无属性挣扎、低 HP、多段命中、失效画皮/结冻头，以及款待回复满血后恢复多重鳞片。结冻头物种在当前 M-C 不合法，其场景只验证机制，不进入合法推荐样本。

| 桌面流程 | 实际检查 |
|---|---|
| 核心构筑 | 搜索真实配置、锁核心、补齐、预览、应用、撤销/重做、导出与重启恢复 |
| 完整队伍编辑 | 导入、错误配点、改动预览、伤害条件、模拟期间编辑与取消 |
| 算法升级 | 缺版本字段的旧模型升级，自定义语料重建、进度、取消与旧记录保留 |
| 规则更新 | 本地编译、能力值/伤害差异、候选激活、旧快照保留、迁移冲突与分表设置隔离 |
| 速度与替换 | 具体配置速度、条件保留、笔记与只替换指定位置 |
| 计划与记录 | 保存两条路线、实战快照、元数据编辑保留建议、重启恢复 |
| 条件模板 | 天气失去、90% HP 等三场景持久化，风险比较与配点复用 |
| 来源与维护 | 原文/解读核对、偏好保存、存储报告、清理预览与执行 |
| 输入与布局 | 后台构筑期间输入、键盘滚动、焦点稳定、840×890 窗口和 125% 缩放 |

Axe 检查工作台、成员编辑和环境页的 WCAG 2 A/AA、2.1 A/AA 自动规则，所测状态没有违规项。Electron 单文档使用 legacy aggregation 模式；这不是人工读屏验收。

## 配置与构筑基线

外层按时间切分，截止 `2026-09-10T16:00:00.000Z`，训练 1,927 份来源，留出 48 支队伍、288 份配置；隔离重复队伍、作者和原链接，排除未来历史观察。该窗口已用于开发，**不是新的独立盲测**。

| 方法 | 道具命中 | 特性命中 | 招式集合 Jaccard | 完整配置命中 | 六维点数 MAE |
|---|---:|---:|---:|---:|---:|
| 热门真实配置 | 57.99% | 93.06% | 0.610 | 3.125% | 5.236 |
| 旧固定三类聚类 | 58.68% | 93.40% | 0.640 | 1.39% | 4.814 |
| 新条件配置模型 | 56.60% | 87.50% | 0.564 | 4.17% | 5.520 |

完整命中要求招式、道具、特性、性格和六维点数全部一致；MAE 单位为 Champions 能力点。新模型完整配置命中略高，但道具、特性、招式和配点误差没有优势，不能只报告有利指标。

从 6 支留出队伍的前两只核心补齐，使用相同对手、随机种子和三种对手策略复评。每种方法完成 144 场模拟：

| 方法 | 模拟胜场 / 完成场数 | 剩余物种复原 | 完整队伍合法 |
|---|---:|---:|---:|
| 新搜索 | **69 / 144** | 20.83% | 6 / 6 |
| 热门配置补齐 | **85 / 144** | 41.67% | 6 / 6 |
| 旧推荐启发式 | **83 / 144** | 8.33% | 6 / 6 |

旧启发式直接调用参考工程的原推荐函数；旧聚类按其固定三类、等权去重距离移植。三种方法使用同一训练候选和规则校验。新搜索在这组开发用例中更差，现有证据不支持整体强度提升。构筑复原只衡量与原队伍剩余物种的一致程度。逐案例与置信区间见 [benchmark.json](reports/benchmark-mc-1.3.0.json)。

## 概率校准、部分观察与候选预算

道具、特性、完整招式集合各预测一次，共 864 个字段目标。温度在内部留出上选择，外层报告结果；消融保持同一训练标签空间，未获支持标签仍保留概率。

| 实验 | log loss ↓ | Brier ↓ | 字段命中率 |
|---|---:|---:|---:|
| 未温度校准 | 1.16172 | 0.46334 | 61.34% |
| 内部留出温度校准 | 1.15044 | 0.45902 | 60.53% |
| 移除部分字段证据 | 1.15306 | 0.46049 | 60.07% |
| 移除历史先验 | **1.12347** | **0.45222** | **61.57%** |

本次校准改善概率损失、降低命中率；移除历史先验反而更好。不能据此宣称历史迁移已经产生净收益，也没有根据这一外层结果重新调参后再重复声称盲测。

| 遮蔽字段 | 兼容流派分配 | 训练中可识别的 71 份配置：真实流派概率质量 |
|---|---:|---:|
| 配点 | 186 / 288 | 1.0000 |
| 配点、性格 | 206 / 288 | 0.9614 |
| 配点、性格、道具 | 225 / 288 | 0.9108 |

三个实验均保持真实 medoid 不变，缺失配点不补零。右列仅对训练中存在完全相同配置的 71 份观察计算，不能当作全部新配置的准确率。

候选消融使用预算 24/48/96、束宽 4/7/12 和两个核心。18 组无时间截止实验均产生合法方案，用时 1.35–2.98 秒；另 18 组统一为 3,000 ms，**17 组产生完整合法方案，1 组在成队前用尽预算**，该组没有被计作合法或成功结果。其实际用时 3,009.3 ms，报告保留 9.3 ms 超时。69 个已知低频技术配置中召回 8–12 个，用户指定变体保留检查通过。默认应用搜索没有强加这项实验时间截止。详见 [research-benchmark.json](reports/research-benchmark-mc-1.3.0.json)。

## 策略、信息状态与失败日志

每类选一支真实当前队伍与另一支真实队伍比较；每种策略固定 8 场训练、6 场复评，使用独立复评种子。这些是机制覆盖开发用例，不代表环境整体分布。

| 真实队伍类别 | 混合策略 | 伤害策略 | 支援策略 |
|---|---:|---:|---:|
| 灭歌 | 4 / 6 | 1 / 6 | 2 / 6 |
| 复活 | 1 / 6 | 2 / 6 | 1 / 6 |
| 空间 | 0 / 6 | 0 / 6 | 3 / 6 |
| 场地轻装 | 3 / 6 | 3 / 6 | 1 / 6 |
| 晴天 | 1 / 6 | 5 / 6 | 3 / 6 |
| 常规轮转 | 5 / 6 | 0 / 6 | 3 / 6 |
| 合计 | 14 / 36 | 11 / 36 | 13 / 36 |

同 3 秒预算实验分别为混合 81/207、伤害 51/124、支援 53/121；各有 5 场未完成，全部排除出胜负统计。时间预算的吞吐量受机器状态影响，不能像固定场数一样逐场复现。完整日志见 [strategy-benchmark.json](reports/strategy-benchmark-mc-1.3.0.json)。

信息状态编码在固定策略和配置模型下单独消融。新旧编码固定场数均为 **11/36 胜、18/357 次复评决策复用**，没有观察到复用或成绩改善。同时间实验为旧编码 121/310、新编码 125/320，未完成分别 2/3 场；访问分布和训练数量保存在 [information-state-benchmark.json](reports/information-state-benchmark-mc-1.3.0.json)，不能仅凭场数增加推断策略更强。

[本版失败核对](reports/policy-failure-review-mc-1.3.0.json)逐行检查真实公开协议，**不是外部专家人工复盘**：

- MC101，支援策略第 2 场：未选出耿鬼、没有执行灭歌；卡比兽地裂落空后落败。
- MC111，支援策略第 2 场：巴布土拨第 1 回合倒下，没有使用复生祈祷。
- MC100，支援策略第 1 场：对手场地确实触发青草种子，但西狮海壬仍在第 1 回合被集火击倒。

这些结果说明特殊资源与首发策略仍有缺陷。默认混合策略、多策略复评和日志入口已经实现，不能因此称为高水平对战策略。

## 作者意图与独立新窗口

历史队报取自 [MegaHappyDragonite 的 M-B 构筑](https://www.smogon.com/forums/threads/vgc-teams-of-the-week-accepting-regulation-m-b.3727451/page-4#post-11086322)及其 [PokePaste](https://pokepast.es/4bd0f376f5730e32)，核对作者关于空间、剑舞掩护、款待恢复多重鳞片、围巾空间外模式和击掌开空间的五条原文。

原文的 Mega 大嘴娃大力士不能代表进化前特性，解析保持未知。评测枚举三种合法初始特性，明确标为生成假设后分别校验 M-C；根据作者机制构造的四人路线均排快速评价第 2。**作者没有提供实际逐局选出或胜负，这不是实际选出复原率**。历史用例未混入 M-C 频率。见 [source-intent-benchmark.json](reports/source-intent-benchmark-mc-1.3.0.json)。

新窗口先保存 [冻结协议](reports/external-holdout-protocol.json)及 bootstrap SHA-256，再读取 9 月 13 日 DreamPrince 发布的坚盾剑怪配置；执行完整配置查重（不把未提供性别当作新配置）、赛季/日期、作者和来源隔离。

- 实际新增 **1 份完整单只配置、0 支完整队伍**，没有编造其余队友、首发或胜负。
- 训练中另排除 `791705272:PC517`：日期 `29th April 2026` 无法由现有日期函数解析；不是因为相关作者，具体原因保留在报告中。
- 三个字段目标的 log loss 为 2.46661，Brier 为 0.74733，命中 1/3；移除历史为 2.56554 / 0.82915，移除部分观察为 2.43935 / 0.74277。
- 新配置没有出现在训练完整配置中。冻结后的外部标签未用于调整温度、先验或算法；观察过结果后，该窗口不再称为未见测试集。

一份配置不足以证明模型优势，更不能证明实战强度。[外部报告](reports/external-benchmark.json)包含输入哈希校验、训练排除原因、预测和消融。

## 性能与存储

| 操作 | 当前机器结果 | 实际含义 |
|---|---|---|
| 热缓存搭档推荐，5 次 | 51.9–55.9 ms，中位 54.4 ms | 满足快速建议约 3 秒目标 |
| 两只核心补齐，6 次 | 0.99–4.64 秒，中位 3.05 秒 | 部分超过 3 秒目标 |
| 后台构筑期间搜索/编辑，26 样本 | 中位 28.7 ms，P95 31.6 ms，最大 31.7 ms | input 到第二次 RAF 的反馈代理指标 |
| 实际本地规则准备及迁移阶段 | 39.7 秒 | 含编译、校验和模型重建，来自隔离存储循环 |
| 深入评估 | 短预算已有完整复评；计算期间可编辑与取消 | 更长预算入口可用，短实验不证明长期策略质量 |

[输入延迟记录](reports/input-latency-0.2.0.json)使用真实 Electron 键盘事件，但在 X11/Xvfb 和 `POKE_TEST` 下关闭硬件加速；**不是物理按键到屏幕的端到端延迟**。真实中文输入法另行验证，不能用推荐耗时代替输入反馈耗时。

[存储复测](reports/storage-benchmark.json)在隔离数据库中执行实际本地规则编译、4 次在线同步、24 次新旧快照切换，共 **119.26 秒**。两份队伍保留，相同内容同步复用模型；显式清理后磁盘 **90.71 → 74.13 MiB**，WAL 为 0，计算上下文全部清空。压缩已修复为先 VACUUM、再 checkpoint(TRUNCATE)，避免压缩后重新留下大 WAL。

采样峰值 RSS 为约 1,691 MiB；Worker 共享进程 RSS 不叠加。清理后队伍、原文和规则仍保留；不承诺 RSS 立即归还系统。这是约 2 分钟的真实循环，**不是多小时稳定性证明**。

## Linux 发布与普通桌面启动

最终文件位于 `release/0.2.0/`，保留旧版本目录。AppImage 直接通过 FUSE 启动，tar.gz 解压到独立临时目录后运行，未用开发模式代替成品验证。

| 成品 | 检查范围 |
|---|---|
| `linux-unpacked/poke-teambuilder` | 完整 14 项普通桌面流程，另行执行真实系统输入法 |
| `poke-teambuilder-0.2.0-x86_64.AppImage` | 旧模型升级、实际规则更新、核心构筑 3 项 |
| `poke-teambuilder-0.2.0-x64.tar.gz` | 解压后执行实际规则更新与核心构筑 2 项 |

规则流程实际禁止轰擂金刚猩、改变暴飞龙攻击种族值、水对岩石相性；逐档对照候选能力值与伤害，再激活、保留旧队伍、创建提示冲突的迁移队伍。新环境修改 gid 不影响旧环境注册表；两份环境分别调用各自引擎。后续数据变化无需修改 UI，机制变化仍需要适配器回归，见 [ENVIRONMENT_UPDATES.md](ENVIRONMENT_UPDATES.md)。

独立 Fcitx5/Rime 检查使用隔离 X11/Xvfb/DBus、系统词库和 XTest 原生按键，实际输入“皮卡丘”，记录组合输入开始/结束并保持搜索焦点，未修改用户输入法设置。[输入法记录](reports/ime-validation-0.2.0.json)与[截图](reports/screenshots/system-ime-0.2.0.png)随报告保存；未声称覆盖全部 Wayland 输入法协议。

此外在当前 **niri / Wayland** 桌面启动成品，未设置 `POKE_TEST`、未禁用 GPU。实际完成搜索、锁核心、补齐、当前分析、替换与保存；窗口为平铺窄屏，使用真实搜索与推荐抽屉入口。普通桌面的截图使用 Electron `capturePage`；Playwright 的截图接口在该环境超时，不据此宣称该接口已通过。[启动记录](reports/native-startup-0.2.0.json)包含硬件加速状态、窗口与实际就绪耗时，[窄屏截图](reports/screenshots/workspace-native-0.2.0.png)用于视觉复查。

发布目录与 tar 实际解压目录的 **2,658 个文件**已逐一比较内容及权限；随包保留 63 份生产依赖许可/来源记录，以及 Electron、Showdown、calc 和中文词条声明。最终文件大小、SHA-256、各成品结果和耗时见 [release-manifest.json](reports/release-manifest-0.2.0.json)，校验文件为 [`release/0.2.0/SHA256SUMS`](../release/0.2.0/SHA256SUMS)。旧版 ASAR 校验值保持不变。

检查日志：[核心](reports/checks/core-0.2.0.txt)、[类型](reports/checks/typecheck-0.2.0.txt)、[打包](reports/checks/package-0.2.0.txt)、[运行目录](reports/checks/ui-packaged-0.2.0.txt)、[AppImage](reports/checks/ui-appimage-0.2.0.txt)、[tar 成品](reports/checks/ui-tar-0.2.0.txt)、[系统输入法](reports/checks/ime-0.2.0.txt)、[普通桌面](reports/checks/native-startup-0.2.0.txt)。

## 可复现入口

```bash
pnpm install --frozen-lockfile
pnpm typecheck
timeout 60s pnpm test
pnpm build
xvfb-run -a -s '-screen 0 3200x2000x24' pnpm test:ui
pnpm test:ime
pnpm benchmark
pnpm benchmark:research
pnpm benchmark:information
pnpm benchmark:strategies
pnpm benchmark:source-intent
pnpm benchmark:external
pnpm benchmark:storage
pnpm package:linux
```

复验成品与当前桌面：

```bash
POKE_EXECUTABLE="$PWD/release/0.2.0/linux-unpacked/poke-teambuilder" xvfb-run -a -s '-screen 0 3200x2000x24' pnpm test:ui
POKE_EXECUTABLE="$PWD/release/0.2.0/linux-unpacked/poke-teambuilder" pnpm test:ime
POKE_EXECUTABLE="$PWD/release/0.2.0/poke-teambuilder-0.2.0-x86_64.AppImage" xvfb-run -a -s '-screen 0 3200x2000x24' pnpm exec playwright test tests/desktop/configurations.spec.ts tests/desktop/environment.spec.ts tests/desktop/workspace.spec.ts --grep '应用升级|实际本地|核心构筑'
node tools/smoke-release.mjs
```

`pnpm benchmark` 需要参考目录 `../poke-type`，或设置 `POKE_REFERENCE_DIR`；应用本身不依赖旧工程。系统输入法检查需要 Xvfb、DBus、Fcitx5、Rime 和 `rime_deployer`。普通桌面检查使用临时数据目录，仅关闭本次启动的窗口。

## 尚未得到的证明

- 新推荐优于热门配置或旧启发式；新窗口完整队伍盲测、高水平玩家实战对照和外部专家逐场复盘。
- 所有合法招式/特性组合的机制一致性；当前仅证明列出的回归场景。
- 人工读屏、物理多显示器、不同显卡/发行版和多小时连续运行。
- 所有完整构筑三秒内完成，或物理输入反馈始终低于 100 ms。
- Windows/macOS、BO3 跨局策略、云同步、公网自动升级与发布签名。
