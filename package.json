{
  "type": "module",
  "scripts": {
    "check": "node --check static/app/main.js",
    "share-renderer:build": "cmake -S tools/poketeam-display -B tools/poketeam-display/build && cmake --build tools/poketeam-display/build --target poketeam_display",
    "desktop:prepare": "node tools/prepare-tauri-assets.mjs",
    "desktop:prepare:dev": "node tools/prepare-tauri-assets.mjs --dev",
    "desktop:prepare:check": "node tools/prepare-tauri-assets.mjs --check",
    "desktop:build": "npm run desktop:prepare && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3"
  }
}
